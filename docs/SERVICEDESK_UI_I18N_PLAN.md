# ServiceDesk UI: план мультиязычности и редактора переводов

## Назначение документа

Этот документ нужен как единая опорная точка для всех последующих сессий по внедрению мультиязычности в UI ServiceDesk.

Цели:

- перевести интерфейс с текущего русского на английский;
- заложить архитектуру для добавления новых языков без переделки UI;
- централизовать все пользовательские тексты, даты, статусы, уведомления и подписи;
- подготовить основу для будущего редактора переводов прямо из UI;
- вести работу итеративно, но без архитектурного долга.

---

## Готовый промпт для новой сессии

Ниже промпт, который можно использовать в начале новой сессии почти без изменений.

```text
Мы продолжаем разработку мультиязычности UI для ServiceDesk в репозитории goSSS.

Контекст и обязательные правила:

1. Основной документ плана находится в файле docs/SERVICEDESK_UI_I18N_PLAN.md. Сначала прочитай его и сверяйся с ним в течение всей работы.
2. Весь UI должен поддерживать мультиязычность через универсальную архитектуру, а не точечные замены строк.
3. Базовые языки на первом этапе: en и ru.
4. Английский должен стать целевым основным интерфейсом, но русский должен продолжать работать как альтернативная локаль.
5. Все новые пользовательские строки, подписи, placeholders, validation messages, уведомления, modal/popconfirm тексты, aria-label, статусы и форматирование дат должны идти только через слой локализации.
6. Нельзя оставлять новые захардкоженные пользовательские строки в компонентах.
7. При необходимости локаль должна синхронизироваться с profile_config пользователя.
8. Нужно учитывать интеграцию с Ant Design locale и dayjs locale.
9. Тексты ошибок от backend нельзя бесконтрольно показывать как единственный источник локализованного текста. Нужен безопасный fallback и, где возможно, маппинг кодов/типов ошибок.
10. Архитектура должна подготовить проект к будущему редактору переводов из UI.

Что нужно сделать в этой сессии:

- сначала кратко зафиксируй, какой пункт плана из docs/SERVICEDESK_UI_I18N_PLAN.md ты реализуешь сейчас;
- затем проверь текущие файлы, связанные с этим этапом;
- после этого внеси реальные изменения в код;
- если меняешь UX-тексты, переведи их на i18n-ключи и добавь переводы минимум для en и ru;
- если меняешь форматирование даты/времени, приведи его к общей locale-aware схеме;
- если меняешь тесты, предпочитай устойчивые селекторы и явную фиксацию языка в тестовой среде;
- в конце обнови docs/SERVICEDESK_UI_I18N_PLAN.md: пометь выполненные пункты, коротко зафиксируй прогресс, новые решения, риски и следующий рекомендуемый шаг.

Формат работы:

- не делай абстрактный обзор вместо изменений;
- работай поэтапно и аккуратно;
- не ломай существующую функциональность;
- если видишь архитектурный риск или зависимость от backend, зафиксируй это в документе;
- после завершения перечисли, что именно сделано, что осталось, и какие файлы были затронуты.
```

---

## Текущее состояние UI

По результатам первичного аудита:

- UI расположен в `front-ui/`;
- стек: React 19, TypeScript, Vite, Ant Design, Zustand, TanStack Query, dayjs;
- сейчас локализация отсутствует как системный слой;
- значительная часть текстов зашита прямо в компонентах и страницах;
- `ConfigProvider` Ant Design уже использует жёстко заданную русскую локаль;
- форматирование дат выполняется смешанно: через `dayjs(...).format(...)` и `new Date(...).toLocaleString()`;
- тесты местами завязаны на русские тексты;
- часть ошибок приходит как свободный текст от backend, что создаёт риск для английского UI.

Ключевые проблемные зоны:

- layout и навигация;
- фильтры, таблицы, формы, placeholders;
- статусы и enum-метаданные;
- уведомления `message.*`, `Modal.*`, `Popconfirm`;
- форматирование даты/времени;
- тесты, завязанные на текст;
- отображение backend-ошибок;
- отсутствие единого хранилища и стратегии загрузки переводов.

---

## Целевое архитектурное решение

### Библиотеки

Базовый стек локализации:

- `i18next`
- `react-i18next`

Дополнительно:

- locale-модуль Ant Design через `ConfigProvider`;
- locale-модуль `dayjs`.

### Общие принципы

1. Все пользовательские тексты идут через `t(...)`.
2. Используются семантические ключи, а не текстовые ключи.
3. Переводы разбиваются на namespaces.
4. Базовые локали хранятся в репозитории.
5. Архитектура допускает наложение удалённых переводов поверх локальных.
6. Выбранный язык хранится у пользователя в `profile_config.interface.locale`.
7. Есть единый locale registry, который знает:
   - код языка;
   - отображаемое название;
   - locale для Ant Design;
   - locale для dayjs;
   - статус доступности;
   - язык по умолчанию.

### Предлагаемая структура файлов

```text
front-ui/
  src/
    i18n/
      i18n.ts
      supportedLocales.ts
      localeTypes.ts
      loadNamespace.ts
      mergeMessages.ts
      useAppLocale.ts
      formatters.ts
    locales/
      en/
        common.json
        layout.json
        auth.json
        dashboard.json
        tickets.json
        companies.json
        equipment.json
        agents.json
        admin.json
        profile.json
        reports.json
        validation.json
        errors.json
      ru/
        common.json
        layout.json
        auth.json
        dashboard.json
        tickets.json
        companies.json
        equipment.json
        agents.json
        admin.json
        profile.json
        reports.json
        validation.json
        errors.json
```

### Предлагаемые namespaces

- `common` — общие кнопки, действия, лейблы, состояния загрузки, пустые состояния;
- `layout` — меню, header, профиль, тема, уведомления shell;
- `auth` — логин, пароли, авторизация;
- `dashboard` — overview и карточки статистики;
- `tickets` — списки, детали, редактор, история, комментарии, формы;
- `companies`
- `equipment`
- `agents`
- `admin`
- `profile`
- `reports`
- `validation` — типовые сообщения формы;
- `errors` — локализованные fallback-ошибки и маппинги ошибок.

### Формат ключей

Примеры:

- `common.actions.save`
- `common.actions.cancel`
- `common.states.loading`
- `layout.menu.tickets`
- `layout.theme.day`
- `auth.login.submit`
- `tickets.list.columns.number`
- `tickets.details.comments.private`
- `tickets.status.new`
- `validation.required.company`
- `errors.network.default`
- `errors.auth.invalidCredentials`

Не использовать:

- ключи вида `"Войти"`;
- случайные короткие ключи без домена;
- дублирующиеся ключи для одинакового смысла.

---

## Хранение выбранного языка

### Целевое место хранения

В `profile_config.interface.locale`.

Предлагаемое расширение DTO:

```ts
interface UserInterfaceConfigDTO {
  theme_mode?: 'light' | 'dark';
  locale?: 'en' | 'ru' | string;
  theme_palettes?: {
    light?: ThemePaletteConfigDTO;
    dark?: ThemePaletteConfigDTO;
  };
  search?: {
    cards_columns?: number;
  };
}
```

### Правила выбора языка при старте

Порядок:

1. `user.profile_config.interface.locale`, если пользователь авторизован и значение валидно;
2. локально сохранённая locale в браузере, если это допустимо архитектурой;
3. `en` как целевой дефолт;
4. fallback на `ru`, если по каким-то причинам `en` не загружен.

### Переключение языка

Переключение должно:

- менять язык в `i18next`;
- менять locale в Ant Design;
- менять locale в `dayjs`;
- по возможности сохранять выбор в `profile_config`;
- не требовать перезагрузки страницы.

---

## Работа с Ant Design и dayjs

### Ant Design

Нужно убрать жёсткую привязку к одной locale и перейти на выбор locale по текущему языку.

Нужен маппинг примерно такого вида:

```ts
{
  en: enUS,
  ru: ruRU,
}
```

### dayjs

Нужно:

- загрузить locale для используемых языков;
- переключать глобальную locale через `dayjs.locale(...)`;
- постепенно вынести форматирование дат в единый слой.

### Единый слой форматтеров

Нужен набор helper-функций, например:

- `formatDate(value, locale)`
- `formatDateTime(value, locale)`
- `formatRelativeTime(value, locale)`
- `formatDateForTicketStamp(value, locale)`
- `formatLocaleDateTime(value, locale)`

Нельзя оставлять хаотичные вызовы:

- `new Date(...).toLocaleString()`
- `dayjs(...).format('DD.MM.YYYY HH:mm')`

в пользовательских компонентах без общего контекста локали.

---

## Стратегия миграции строк

### Что обязательно переводить

- заголовки страниц;
- подписи полей;
- placeholders;
- кнопки;
- меню;
- теги и статусы;
- empty states;
- уведомления `message.*`;
- `Modal`, `Popconfirm`, `Tooltip`;
- служебные подписи, aria-label, title, helper text;
- строки в карточках, таблицах и деталях;
- fallback-значения вроде `Не назначен`, `Неизвестно`, `Нет описания`.

### Что требует отдельного внимания

- строки, пришедшие с backend;
- названия интеграций и внешних систем;
- значения enum-типов;
- human-readable описание history entries;
- тестовые сценарии, где текст используется как селектор.

### Что не переводится автоматически

- пользовательские данные;
- данные от внешних систем без маппинга;
- свободный текст из тикетов, комментариев и описаний;
- server-side сообщения, если у них нет кода или карты локализации.

---

## Backend-ошибки и fallback-стратегия

Сейчас часть экранов показывает серверный текст как есть. Для мультиязычности это недостаточно.

Целевой подход:

1. Если backend начнёт отдавать стабильный `error_code`, использовать маппинг `error_code -> i18n key`.
2. Если приходит только текст, показывать:
   - локализованный общий fallback;
   - сырой серверный текст только как дополнительную техническую деталь, если это уместно и безопасно.
3. Для известных кейсов ввести frontend-мэппинг:
   - `invalid_credentials`
   - `network_error`
   - `timeout`
   - `unknown_error`

Пример целевого паттерна:

```ts
message.error(resolveApiErrorMessage(error, t));
```

---

## Тестовая стратегия

### Базовые правила

1. Тесты не должны массово зависеть от русских строк.
2. Для функциональных действий лучше использовать:
   - роли;
   - `aria-label`;
   - `data-testid`;
   - фиксированную locale тестовой среды.
3. Для snapshot-подобных и текстовых сценариев язык должен быть явно зафиксирован.

### Целевой подход

- в тестовой среде принудительно использовать `en`;
- там, где текст не является сутью сценария, заменить селекторы на устойчивые;
- для компонентов с критически важной локализацией добавить unit/smoke-тесты на переключение языка.

### Что проверить тестами

- смена языка обновляет UI без перезагрузки;
- Ant Design контролы меняют locale;
- даты форматируются в соответствии с выбранным языком;
- сохранение языка в профиле работает корректно;
- missing key не ломает UI;
- fallback `en -> ru` или `ru -> en` работает по выбранной стратегии.

---

## План реализации по этапам

Ниже перечислены этапы в рекомендуемом порядке. Следующий этап начинать только после стабилизации предыдущего.

### Этап 0. Подготовка и инвентаризация

Цель:

- формализовать правила локализации и зафиксировать опорную структуру.

Задачи:

- создать этот документ;
- создать список целевых namespaces;
- зафиксировать правила именования ключей;
- зафиксировать язык по умолчанию;
- согласовать место хранения locale в профиле;
- определить политику fallback-ошибок;
- определить тестовую стратегию.

Критерий готовности:

- команда опирается на единый документ и не принимает локальные архитектурные решения “по месту”.

### Этап 1. Базовая i18n-инфраструктура

Цель:

- добавить в проект рабочую инфраструктуру мультиязычности без массовой миграции компонентов.

Задачи:

- установить `i18next` и `react-i18next`;
- создать `src/i18n/i18n.ts`;
- создать `src/i18n/supportedLocales.ts`;
- создать базовые namespaces `common` и `layout` для `en` и `ru`;
- инициализировать `I18nextProvider`/`initReactI18next`;
- интегрировать locale registry с Ant Design;
- интегрировать locale registry с `dayjs`;
- добавить helper `useAppLocale`;
- подключить всё в корневом приложении.

Критерий готовности:

- язык можно переключить программно;
- Ant Design и `dayjs` синхронно используют выбранную locale;
- есть базовые translation files для `en` и `ru`.

### Этап 2. User locale и настройка профиля

Цель:

- сделать язык частью пользовательского профиля и настроек интерфейса.

Задачи:

- расширить фронтовые DTO под `profile_config.interface.locale`;
- добавить переключатель языка в профиль или header;
- сохранять смену языка в профиль пользователя;
- корректно восстанавливать язык при логине и обновлении страницы;
- определить поведение для неавторизованного пользователя.

Критерий готовности:

- пользователь меняет язык один раз и дальше работает в нём без ручного повторного выбора.

### Этап 3. Shared UI и shell

Цель:

- перевести все общие элементы интерфейса на i18n.

Задачи:

- перевести `App.tsx`, `MainLayout.tsx`, `HeaderSearch.tsx`;
- вынести меню, theme labels, user menu, уведомления, aria-label;
- локализовать общие actions и states;
- убрать жёсткие русские строки из layout-компонентов;
- нормализовать общие placeholders и common buttons.

Критерий готовности:

- shell приложения и навигация переключаются между `ru` и `en`.

### Этап 4. Статусы, enum и общие справочники

Цель:

- убрать текстовые label из констант и справочников.

Задачи:

- перевести `ticketStatus.ts` на `labelKey`;
- аналогично обработать другие enum/meta места;
- добавить helper-функции вида `getTicketStatusMeta(t, status)`;
- убрать прямые русские метки из reusable utilities.

Критерий готовности:

- статусы и справочники не содержат пользовательских строк, кроме ключей.

### Этап 5. Тикетный модуль

Цель:

- перевести самый крупный и критичный пользовательский модуль.

Задачи:

- перевести `TicketsPage.tsx`;
- перевести `TicketDetailsPage.tsx`;
- перевести `NewTicketModal.tsx`;
- перевести editor-related UI, комментарии, историю, карточки, фильтры, popconfirm, modals;
- вынести функции human-readable label в i18n-aware слой;
- заменить разрозненные fallback-строки на ключи.

Критерий готовности:

- полный поток работы с тикетом доступен на `en` и `ru`.

### Этап 6. Остальные доменные разделы

Цель:

- перевести остальной UI блоками без смешивания ответственности.

Порядок:

1. `dashboard`
2. `companies`
3. `equipment`
4. `agents`
5. `admin`
6. `reports`
7. `profile`

Критерий готовности:

- во всех основных страницах пользовательский текст берётся из i18n.

### Этап 7. Форматтеры, ошибки и cleanup

Цель:

- убрать технический разнобой и стабилизировать результат.

Задачи:

- централизовать форматирование даты/времени;
- минимизировать прямые `toLocaleString()` и локальные `dayjs.format(...)`;
- внедрить `resolveApiErrorMessage`;
- убрать остаточные русские строки из UI-кода;
- проверить aria-label и title-атрибуты;
- добавить dev-проверки на missing translations.

Критерий готовности:

- проект не содержит несанкционированных UI-строк в рабочих модулях.

### Этап 8. Тесты и качество

Цель:

- сделать мультиязычность устойчивой и проверяемой.

Задачи:

- адаптировать существующие smoke/unit-тесты;
- зафиксировать locale тестовой среды;
- ввести тесты на переключение языка;
- проверить критичные формы и уведомления;
- проверить отсутствие регрессий в tickets flow.

Критерий готовности:

- тесты стабильно проходят при предсказуемой locale;
- критичные сценарии покрыты.

### Этап 9. Подготовка к удалённым переводам

Цель:

- подготовить UI к будущему редактору переводов без ломки базовой схемы.

Задачи:

- ввести абстракцию загрузки namespace-переводов;
- отделить локальные базовые ресурсы от потенциальных remote overrides;
- определить merge-стратегию:
  - base locale из репозитория;
  - remote overrides из backend;
  - runtime cache.

Критерий готовности:

- UI умеет получать переводы не только из локальных JSON, хотя backend ещё может быть не реализован.

### Этап 10. Редактор переводов в UI

Цель:

- дать возможность управлять переводами из интерфейса.

Это конечный этап, запускать его только после стабилизации базовой мультиязычности.

#### Функциональная цель редактора

Редактор должен позволять:

- выбирать язык;
- выбирать namespace;
- видеть список ключей и текущие значения;
- искать по ключу и по тексту;
- видеть, где перевод отсутствует;
- редактировать переводы;
- сохранять переводы;
- просматривать diff относительно базового перевода;
- фильтровать только missing/changed keys;
- при необходимости публиковать изменения.

#### Минимальная целевая модель данных

Базовая запись перевода:

```ts
type TranslationEntry = {
  locale: string;
  namespace: string;
  key: string;
  value: string;
  base_value?: string;
  status?: 'base' | 'override' | 'missing' | 'changed';
  updated_at?: string;
  updated_by?: string;
};
```

#### Предпочтительная архитектура

Источники:

1. base translations из репозитория;
2. override translations из backend;
3. результирующий merged view в UI.

#### Рекомендуемый backend-контур для будущего

Примерный API-контур:

- `GET /i18n/locales`
- `GET /i18n/namespaces`
- `GET /i18n/translations?locale=en&namespace=tickets`
- `PATCH /i18n/translations`
- `POST /i18n/translations/publish`
- `GET /i18n/translations/diff?locale=en&namespace=tickets`

#### Рекомендуемые роли доступа

- только администратор или отдельная роль локализации;
- readonly режим для остальных;
- аудит изменений обязателен.

#### UX редактора переводов

Нужны:

- таблица ключей;
- inline editor;
- индикатор missing;
- diff-view;
- bulk-save;
- кнопка “загрузить базовые ключи”;
- фильтр по namespace;
- поиск по ключу и по переводу;
- предупреждение о несохранённых изменениях.

#### Обязательные проверки редактора

- нельзя терять base translation;
- пустые строки должны валидироваться по политике проекта;
- одинаковые ключи в разных namespaces не должны конфликтовать;
- публикация должна быть отслеживаемой;
- загрузка remote overrides не должна ломать локальный fallback.

Критерий готовности:

- переводами можно управлять из UI без правки файлов в репозитории;
- изменения безопасно применяются поверх базовых локалей.

---

## Технические соглашения для реализации

### Что делать обязательно

- добавлять переводы сразу для `en` и `ru`;
- при миграции компонента убирать из него захардкоженные UI-строки;
- использовать единый helper для ошибок;
- использовать единый слой форматирования дат;
- обновлять этот документ после каждой значимой итерации.

### Что не делать

- не смешивать ключи и тексты;
- не писать новый UI-текст прямо в JSX;
- не плодить дублирующие namespaces;
- не использовать разрозненные ad hoc функции локализации;
- не внедрять редактор переводов до завершения базовой миграции.

---

## Definition of Done для всей инициативы

Инициатива считается завершённой по базовой части, когда:

- интерфейс ServiceDesk работает минимум на `en` и `ru`;
- локаль выбирается и сохраняется корректно;
- Ant Design и `dayjs` синхронизированы с выбранным языком;
- основные пользовательские разделы не содержат захардкоженных UI-строк;
- статусы, меню, формы, уведомления и даты локализуются централизованно;
- тесты адаптированы к новой архитектуре;
- есть инфраструктура для future remote overrides;
- есть зафиксированный дизайн редактора переводов.

Инициатива считается завершённой полностью, когда:

- редактор переводов реализован;
- переводы можно безопасно редактировать из UI;
- override-модель и аудит изменений работают стабильно.

---

## Журнал прогресса

Этот раздел нужно обновлять после каждой значимой сессии.

### 2026-03-25

Сделано:

- проведён первичный аудит структуры `front-ui`;
- зафиксирована целевая архитектура мультиязычности;
- выбран общий подход: `i18next` + `react-i18next` + locale registry + синхронизация с Ant Design и `dayjs`;
- определён поэтапный план миграции;
- зафиксирован конечный контур редактора переводов в UI;
- создан этот опорный документ.

Следующий рекомендуемый шаг:

- начать Этап 1: добавить базовую i18n-инфраструктуру в `front-ui` без массовой миграции компонентов.

Риски:

- backend пока может отдавать текст ошибок без стабильных кодов;
- часть тестов завязана на русские строки;
- в проекте уже есть locale-зависимая логика поиска и форматирования, которую нужно аккуратно выровнять.

